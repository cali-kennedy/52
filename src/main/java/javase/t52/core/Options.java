package main.java.javase.t52.core;

/**
 * Option defines the possible actions a player (or dealer) can take during a betting round.
 */
public enum Options {
    CHECK,
    CALL,
    RAISE,
    FOLD;
}
